const products = [
  { name: "Laptop", price: 1500, categories: ["Electronics", "Computers"] },
  { name: "Shirt", price: 20, categories: ["Clothing"] },
  { name: "Phone", price: 500, categories: ["Electronics", "Mobile"] },
  { name: "Mug", price: 5, categories: ["Kitchen"] },
];

// price > 20
const mapFunction = (arr) => {
  //   let result;
  //   result = arr.map((val) => val);
  //   let filtered = result.filter(
  //     (itm) => itm.price > 20 && itm.categories === "Electronics"
  //   );
  //   console.log(filtered, "result");
  // return result
  //   let score = arr.filter(
  //     (val) => val.scores.math > 80 || val.scores.science > 80
  //   );
  //   console.log(score, "score");
  // for (let i = 0; i < arr.length; i++) {
  //   const element = arr[i];
  //   console.log(element, "element");
  // }
};

mapFunction(students);
